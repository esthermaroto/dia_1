<?php
// Detectar si el usuario está logueado y si es admin
$isLogged = isset($_SESSION['user']) && $_SESSION['user'] !== false;
$isAdmin = $isLogged && $_SESSION['user']->getRol() == 0;
?>
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tienda</title>
    <link rel="stylesheet" href="styles/style.css">
    <link rel="stylesheet" href="styles/shop.css">
    <style>
        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        .product-card {
            border: 1px solid #ccc;
            border-radius: 10px;
            padding: 15px;
            text-align: center;
            background-color: #f9f9f9;
        }
        .product-card img {
            max-width: 100%;
            height: 150px;
            object-fit: cover;
            border-radius: 5px;
        }
        .product-card h2 {
            font-size: 1.2em;
            margin: 10px 0 5px 0;
        }
        .product-card p {
            margin: 5px 0;
        }
        .price {
            font-weight: bold;
            color: #2c3e50;
        }
        .availability {
            margin: 5px 0;
        }
        .btn, .btn-secondary, .btn-danger {
            display: inline-block;
            padding: 5px 10px;
            margin: 3px 2px;
            text-decoration: none;
            border-radius: 5px;
            font-size: 0.9em;
        }
        .btn { background-color: #3498db; color: #fff; }
        .btn:hover { background-color: #2980b9; }
        .btn-secondary { background-color: #2ecc71; color: #fff; }
        .btn-secondary:hover { background-color: #27ae60; }
        .btn-danger { background-color: #e74c3c; color: #fff; }
        .btn-danger:hover { background-color: #c0392b; }
        .nav-links {
            margin-bottom: 20px;
        }
        .nav-links a {
            margin-right: 10px;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="main-container">
        <h1>Tienda</h1>

        <div class="nav-links">
            <?php if($isAdmin): ?>
                <a href="index.php?c=shop&action=newProduct" class="btn-secondary">Agregar nuevo producto</a>
                <a href="index.php?c=cart&action=view" class="btn">Ver carrito</a>
                <a href="index.php?c=register&logout=1" class="btn-danger">Cerrar sesión</a>
            <?php elseif($isLogged): ?>
                <a href="index.php?c=cart&action=view" class="btn">Ver carrito</a>
                <a href="index.php?c=register&logout=1" class="btn-danger">Cerrar sesión</a>
            <?php else: ?>
                <a href="index.php?c=register" class="btn-secondary">Iniciar sesión / Registrarse</a>
            <?php endif; ?>
        </div>

        <?php if(isset($_GET['message'])): ?>
            <?php if($_GET['message'] == 'created'): ?>
                <p style="color: green;">✅ Producto creado correctamente.</p>
            <?php elseif($_GET['message'] == 'updated'): ?>
            <p style="color: green;">✅ Producto actualizado correctamente.</p>
            <?php elseif($_GET['message'] == 'deleted'): ?>
                <p style="color: red;">✅ Producto eliminado correctamente.</p>
            <?php endif; ?>
        <?php endif; ?>

        <form action="index.php?c=shop" method="POST">
            <input type="text" name="search" placeholder="Buscar producto" />
            <input type="submit" name="buscar" value="Buscar" class="btn" />
        </form>

        <div class="product-grid">
            <?php foreach ($productos as $producto): ?>
                <div class="product-card">
                    <img src="img/productos/<?php echo $producto->getImagen(); ?>" alt="<?php echo $producto->getName(); ?>" />
                    <h2><?php echo $producto->getName(); ?></h2>
                    <p><?php echo $producto->getDescription(); ?></p>
                    <p class="price"><?php echo number_format($producto->getPrice(), 2); ?> €</p>
                    <p class="availability">
                        <?php echo ($producto->getStock() > 0) ? '✅ Disponible' : '❌ Agotado'; ?>
                    </p>

                    <?php if($isAdmin): ?>
                        <a href="index.php?c=cart&action=add&id=<?php echo $producto->getIdProducto(); ?>" class="btn">Añadir al carrito</a>
                        <br>
                        <a href="index.php?c=shop&action=editProduct&id=<?php echo $producto->getIdProducto(); ?>" class="btn-secondary">Editar</a>
                        <a href="index.php?c=shop&action=deleteProduct&id=<?php echo $producto->getIdProducto(); ?>" class="btn-danger"
                           onclick="return confirm('¿Seguro que quieres eliminar este producto?');">Eliminar</a>
                    <?php elseif($isLogged): ?>
                        <a href="index.php?c=cart&action=add&id=<?php echo $producto->getIdProducto(); ?>" class="btn">Añadir al carrito</a>
                    <?php endif; ?>
                </div>
            <?php endforeach; ?>
        </div>
    </div>
</body>
</html>
